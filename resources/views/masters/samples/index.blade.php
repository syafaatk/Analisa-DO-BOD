<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Master Sample</title><style>body{font-family:Arial;margin:32px;background:#f5f7fb;color:#172033}.card{background:#fff;padding:20px;border-radius:12px;margin-bottom:20px;box-shadow:0 2px 10px #0001}input,textarea,button{padding:10px;margin:4px;border:1px solid #ccd3df;border-radius:7px}button{background:#173f7a;color:#fff;cursor:pointer}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}table{width:100%;border-collapse:collapse}th,td{padding:9px;border-bottom:1px solid #e5e8ee;text-align:left}.nav a{margin-right:15px}</style></head><body><div class="nav"><a href="{{route('analysis.index')}}">Analisis</a><a href="{{route('masters.samples')}}">Sample</a><a href="{{route('masters.reagents')}}">Reagen</a><a href="{{route('masters.instruments')}}">Instrumen</a></div><h1>Master Sample</h1>@if(session('success'))<div class="card">{{session('success')}}</div>@endif<div class="card"><h3>Tambah Sample</h3><form method="post" action="{{route('masters.samples.store')}}">@csrf<div class="grid"><input name="sample_code" placeholder="Kode sample *" required><input name="sample_name" placeholder="Nama sample"><input name="matrix" placeholder="Matrix"><input name="received_at" type="datetime-local"><input name="customer" placeholder="Customer"><input name="notes" placeholder="Catatan"></div><button>Simpan</button></form></div><div class="card"><table><tr><th>Kode</th><th>Nama</th><th>Matrix</th><th>Customer</th><th>Aksi</th></tr>@foreach($samples as $s)<tr><td>{{$s->sample_code}}</td><td>{{$s->sample_name}}</td><td>{{$s->matrix}}</td><td>{{$s->customer}}</td><td><form method="post" action="{{route('masters.samples.delete',$s)}}" onsubmit="return confirm('Hapus sample?')">@csrf @method('DELETE')<button>Hapus</button></form></td></tr>@endforeach</table>{{$samples->links()}}</div></body></html>
