<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Master Instrumen</title><style>body{font-family:Arial;margin:32px;background:#f5f7fb}.card{background:#fff;padding:20px;border-radius:12px;margin-bottom:20px;box-shadow:0 2px 10px #0001}input,select,button{padding:10px;margin:4px;border:1px solid #ccd3df;border-radius:7px}button{background:#173f7a;color:#fff}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}table{width:100%;border-collapse:collapse}th,td{padding:9px;border-bottom:1px solid #e5e8ee;text-align:left}.nav a{margin-right:15px}</style></head><body><div class="nav"><a href="{{route('analysis.index')}}">Analisis</a><a href="{{route('masters.samples')}}">Sample</a><a href="{{route('masters.reagents')}}">Reagen</a><a href="{{route('masters.instruments')}}">Instrumen</a></div><h1>Master Instrumen</h1>@if(session('success'))<div class="card">{{session('success')}}</div>@endif<div class="card"><form method="post" action="{{route('masters.instruments.store')}}">@csrf<div class="grid"><input name="code" placeholder="Kode *" required><input name="name" placeholder="Nama instrumen *" required><input name="serial_number" placeholder="Serial number"><input name="calibration_due" type="date"><select name="status"><option value="active">Active</option><option value="maintenance">Maintenance</option><option value="expired">Expired</option></select></div><button>Simpan</button></form></div><div class="card"><table><tr><th>Kode</th><th>Nama</th><th>Serial</th><th>Kalibrasi</th><th>Status</th><th>Aksi</th></tr>@foreach($instruments as $x)<tr><td>{{$x->code}}</td><td>{{$x->name}}</td><td>{{$x->serial_number}}</td><td>{{$x->calibration_due?->format('Y-m-d')}}</td><td>{{$x->status}}</td><td><form method="post" action="{{route('masters.instruments.delete',$x)}}" onsubmit="return confirm('Hapus instrumen?')">@csrf @method('DELETE')<button>Hapus</button></form></td></tr>@endforeach</table>{{$instruments->links()}}</div></body></html>
