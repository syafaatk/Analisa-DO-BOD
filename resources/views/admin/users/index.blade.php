<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Management User & Role</title><style>body{font-family:Arial;margin:32px;background:#f5f7fb;color:#172033}.card{background:#fff;padding:20px;border-radius:12px;margin-bottom:20px;box-shadow:0 2px 10px #0001}input,select,button{padding:9px;margin:4px;border:1px solid #ccd3df;border-radius:7px}button{background:#173f7a;color:#fff}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}table{width:100%;border-collapse:collapse}th,td{padding:9px;border-bottom:1px solid #e5e8ee;text-align:left}.nav a{margin-right:15px}.role{font-weight:700}</style></head><body><div class="nav"><a href="{{route('analysis.index')}}">Analisis</a><a href="{{route('masters.samples')}}">Sample</a><a href="{{route('masters.reagents')}}">Reagen</a><a href="{{route('masters.instruments')}}">Instrumen</a><a href="{{route('admin.users')}}">Users & Roles</a><form method="post" action="{{route('logout')}}" style="display:inline">@csrf<button>Logout</button></form></div><h1>User & Role Management</h1>@if(session('success'))<div class="card">{{session('success')}}</div>@endif@if(session('error'))<div class="card">{{session('error')}}</div>@endif<div class="card"><h3>Tambah User</h3><form method="post" action="{{route('admin.users.store')}}">@csrf<div class="grid"><input name="name" placeholder="Nama *" required><input name="email" type="email" placeholder="Email *" required><select name="role"><option>analyst</option><option>supervisor</option><option>admin</option><option>viewer</option></select><input name="password" type="password" placeholder="Password min. 8 karakter" required><label><input type="checkbox" name="active" value="1" checked> Aktif</label></div><button>Simpan</button></form></div><div class="card"><table><tr><th>Nama</th><th>Email</th><th>Role</th><th>Status</th><th>Aksi</th></tr>@foreach($users as $u)<tr><td>{{$u->name}}</td><td>{{$u->email}}</td><td class="role">{{$u->role}}</td><td>{{$u->active?'Aktif':'Nonaktif'}}</td><td><form method="post" action="{{route('admin.users.delete',$u)}}" onsubmit="return confirm('Hapus user?')">@csrf @method('DELETE')<button>Hapus</button></form></td></tr>@endforeach</table>{{$users->links()}}</div></body></html>
